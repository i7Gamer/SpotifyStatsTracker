{
  "name": "spotify-stats-tracker-lint",
  "private": true,
  "description": "Dev-only manifest: the app ships no JS bundle and has no runtime node dependency. This exists so the browser scripts in static/js can be linted (see eslint.config.js) - nothing here is installed into the Docker image, which is why .dockerignore excludes it.",
  "scripts": {
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "10.9.1",
    "globals": "17.11.0"
  },
  "overrides": {
    "minimatch": "^10"
  }
}
